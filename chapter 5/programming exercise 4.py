def main():
    acronym = str(input("give me a phrase for me to turn into an acronym"))
    lenght = len(acronym)
    strings = acronym.split(" ")
    data = " "
    for i in range(lenght):
        data = data + strings[i]-



main()